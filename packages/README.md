# Shared Pipeline Packages

This directory will contain reusable components that have been verified in at least one portfolio iteration. Project-specific code should remain under `projects/` until reuse is justified by a second real use case.

The capability ledger in `PORTFOLIO_TRACKING_LOG.md` records where each shared component originated and which later projects reuse it.
