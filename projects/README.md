# Released and Active Projects

Each approved iteration receives its own directory:

```text
WS-001-<project-slug>/
  README.md
  PROJECT_MANIFEST.md
  src/
  tests/
  examples/
  evidence/
```

An iteration is not a released portfolio project until its Manifest, verification evidence, default-branch integration, and tagged release are complete.
